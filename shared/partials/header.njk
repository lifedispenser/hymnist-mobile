<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">
      {% if header.now_playing %}
      <a class="navbar-np" href="playlist.html"><< Now Playing</a>
      {% elif header.search %}
      <a class="navbar-np" href="search.html"><< Search Results</a>
      {% else %}
			<a class="navbar-brand" href="index.html">H</a>
      {% endif %}
      {% if header.title %}
        <div class="navbar-title">{{header.title}}</div>
      {% endif %}
			<a class="navbar-toggle collapsed" href="menu.html">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      {% if header.name %}
        <p class="navbar-text navbar-right">{{header.name}}</p>
      {% endif %}
    </div>
  </div>
</nav>
