<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">

      {% if header.now_playing %}
      <a class="navbar-np" href="playlist.html"><< Now Playing</a>
      {% elif header.current_player %}
      <a class="navbar-np" href="0radio.html"><i class="fa fa-volume-up" aria-hidden="true"></i></a>
      {% elif header.search %}
      <a class="navbar-np" href="search.html"><< Search Results</a>
      {% else %}
			<a class="navbar-brand">H</a>
      {% endif %}
      {% if header.title %}
        <div class="navbar-title">{{header.title}}</div>
      {% endif %}
      {% if header.nomenu == false %}
			<a class="navbar-toggle collapsed" href="0home.html">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      {% endif %}
      {% if header.name %}
        <p class="navbar-text navbar-right">{{header.name}}</p>
      {% endif %}
    </div>
  </div>
</nav>
